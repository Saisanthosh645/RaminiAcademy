/**
 * Mock data - kept for backward compatibility.
 * Production app uses Firebase (usePaidCourses, useUserCourses, useCourse).
 */
import type { ScheduleItem } from "@/types/firebase";

export type { ScheduleItem };
export const mockCourses: unknown[] = [];
